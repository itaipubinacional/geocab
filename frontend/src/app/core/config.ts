/**
 * Classe que representa as configurações do front-end.
 */
export class FrontEndConfiguration {
    /**
     * Armazena as configurações específicas de autenticação a ser utilizada pelo
     * sistema.
     */
    auth: any;
}
